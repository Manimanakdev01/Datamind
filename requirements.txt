# ─────────────────────────────────────────────────────────────
#  DataMind AI — requirements.txt  (Streamlit Cloud)
# ─────────────────────────────────────────────────────────────

# ── Core ─────────────────────────────────────────────────────
streamlit>=1.32.0

# ── Data ─────────────────────────────────────────────────────
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scipy>=1.11.0,<2.0.0

# ── ML ───────────────────────────────────────────────────────
scikit-learn>=1.3.0,<2.0.0

# ── Visualisation ────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Explainability ───────────────────────────────────────────
shap>=0.43.0

# ── Deep Learning ────────────────────────────────────────────
# tensorflow-cpu is smaller (~400 MB) and identical API to tensorflow
tensorflow-cpu>=2.13.0,<2.16.0

# ── Computer Vision ──────────────────────────────────────────
# ── Computer Vision ──────────────────────────────────────────
Pillow>=10.0.0
ultralytics
torch
torchvision
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
Pillow>=10.0.0
ultralytics==8.1.23
requests>=2.31.0
# ── Firebase ─────────────────────────────────────────────────
# pyrebase4 has strict sub-deps — keep it unpinned so pip resolves freely
pyrebase4>=4.7.1

# ── Networking ───────────────────────────────────────────────
requests>=2.28.0,<3.0.0
urllib3>=1.26.0,<3.0.0

# ── QR code ──────────────────────────────────────────────────
qrcode[pil]>=7.4.0

# ── PDF ──────────────────────────────────────────────────────
pdfplumber>=0.9.0
PyPDF2>=3.0.0

# ── Protobuf — pin to avoid pyrebase4 vs tensorflow clash ────
protobuf>=3.20.0,<5.0.0
